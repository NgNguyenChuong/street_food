<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StreetFoodNarrator.App.Views.LegalPage"
             BackgroundColor="#0A1612"
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <Border Grid.Row="0"
                BackgroundColor="#0F1F17"
                Padding="20,16">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="0,0,20,20" />
            </Border.StrokeShape>
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                <Button Grid.Column="0"
                        Text="&#xF004D;"
                        FontFamily="MDI"
                        FontSize="24"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        Clicked="OnBackClicked"
                        WidthRequest="40" />
                <Label x:Name="TitleLabel"
                       Grid.Column="1"
                       Text=""
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="White"
                       VerticalTextAlignment="Center"
                       HorizontalTextAlignment="Center" />
            </Grid>
        </Border>

        <!-- Scrollable content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="16" Padding="20,20,20,40">

                <!-- Section header card -->
                <Border x:Name="IntroCard"
                        BackgroundColor="#0F2D1E"
                        Stroke="#22C55E33"
                        StrokeThickness="1"
                        Padding="16,14">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="14" />
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Label x:Name="IntroIcon"
                               Text="📜"
                               FontSize="28"
                               VerticalTextAlignment="Center" />
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label x:Name="IntroTitle"
                                   FontSize="15"
                                   FontAttributes="Bold"
                                   TextColor="#86EFAC" />
                            <Label x:Name="IntroSubtitle"
                                   Text="Cập nhật: 01/01/2026"
                                   FontSize="11"
                                   TextColor="#64748B" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <!-- Dynamic content sections -->
                <VerticalStackLayout x:Name="ContentStack" Spacing="12" />

                <!-- Footer -->
                <Label Text="Street Food Narrator © 2026 · v1.0.0"
                       FontSize="11"
                       TextColor="#475569"
                       HorizontalTextAlignment="Center"
                       Margin="0,8,0,0" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
